<div class="col-md-8 center-margin">

<%= form_for(@holiday, html: {class: "form-horizontal form-label-left"}) do |f| %>
  <% if @holiday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@holiday.errors.count, "error") %> prohibited this holiday from being saved:</h2>

      <ul>
      <% @holiday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Feriado %>
    <%= f.text_field :name %>

  </div>

  <div class="field">
    <%= f.label :Data %>
    <span class="holiday_date"></span>
    <%= f.date_select :date  %>

  </div>

  <div class="field">
    <%= f.label :Cidade %>
    <%= f.select 'city_id', options_for_select(City.all.collect{ |u| [u.name, u.id] }) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<br>
</div>
